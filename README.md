# cameraLogMaskTool
Zebra requested a tool to generate log masks for override settings. Currently, no customer-facing solution exists, and customers must manually compute masks using documentation. To address this, I developed a binary tool for camera use cases to simplify mask generation and improve efficiency.
